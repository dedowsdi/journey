# posix use System V Unix style
ps -..

# BSD style, no hyphen
ps ..

list process by command name (full match)
ps -C vim

list process by process id, show pid, pgid, ppid, stat, cmd only
ps -123 -o pid,pgid,ppid,stat,cmd

